# Bybit Smart OCO — PRO

Улучшенная 3Commas-настройка на Streamlit с упором на **понятный UI**, **умные OCO-сделки**, **живые статусы**.

## Быстрый старт

```bash
pip install -r requirements.txt
streamlit run bybit_app/app.py
```

## Структура
- `bybit_app/app.py` — главная страница (мультистраничное приложение).
- `bybit_app/pages` — разделы UI.
- `bybit_app/utils` — утилиты (API, логирование, настройки и пр.).
- `bybit_app/_data` — кэш, логи, настройки.

## Примечания
- Для реальной торговли снимите галочку DRY-RUN.
- Testnet переключается на странице «Подключение и состояние».

## Новое в v13+ (апгрейд)
- Реальный WS (public/private) с логированием и монитором.
- Скринер SPOT по 24h объёму и простым метрикам.
- OCO умнее: авто-TP/SL от процента, квантизация по шагам лота/цены, проверка `minNotional`.
- Telegram-уведомление при создании OCO (настроить токен/чат в ⚙️ Настройки).


### В v3
- Авто-отмена второй ноги OCO по событиям Private WS (order/execution).
- Новая страница «Фьючерсы — OCO (Linear)» (BETA).
- Telegram heartbeat (вкл/выкл и период в ⚙️ Настройки и 🤖 Telegram Бот).


## AI-Трейдер (BETA)
- Лёгкая логистическая модель на технических фичах (SMA/EMA/RSI/ATR/волатильность/наклон).
- Обучение на исторических свечах Bybit (`/v5/market/kline`), разбиение train/test, отчёт accuracy.
- Живой контур: раз в 5 секунд оценивает сигнал по выбранным символам и выставляет OCO (Spot) или TP/SL (Linear) через наши хелперы.
- Жёсткие риск-гварды: риск на сделку, дневной лимит убытка, ограничение числа одновременных позиций, DRY-RUN.
- Никаких гарантий прибыли — безопасность и контроль прежде всего.


### В v5
- Partial-fill логика для OCO: TP/SL **автоматически подравнивают qty** под фактически исполненное количество по PRIMARY через `/v5/order/amend`.
- Reconcile: кнопка для сверки групп с биржей (подтянуть qty, закрыть висяки).
- 📟 OCO Дашборд: список групп, исполненная qty, средняя цена, last, оценка PnL.
- WS: авто‑reconnect и ping (run_forever с ping_interval/ping_timeout).
- Кэширование `tickers` и `instruments` через `st.cache_data`.


### В v6 — «умный фильтр сделок»
- **EV-gate**: сделки отправляются только при положительном ожидаемом значении, с учётом комиссий и слиппеджа.
- **Walk-forward backtest** (TimeSeriesSplit) для подбора порогов и проверки устойчивости.
- **Auto-retrain** по расписанию и **дрейф-детектор Page–Hinkley** (заложена точка входа).
- Параметры комиссий/слиппеджа/EV в UI AI-трейдера и странице **🧪 AI Lab**.


### В v8 (SPOT-фокус)
- **Cash-only guards**: резерв кэша, лимит на сделку/символ, привязка размера к доступному USDT (без заимствований).
- **TWAP для спота**: клиентский исполнител, лимит-IOC с нарезкой на срезы и настраиваемой агрессивностью.
- **Spot инвентарь и реализованный PnL**: средняя цена + учёт комиссии из `execution.execFee`.
- Обновлён AI: использует cash-only аллокатор, по крупным сделкам переключается на TWAP.
- Добавлен REST `/v5/market/orderbook` и страница **🧩 TWAP Spot**.


### В v9 (SPOT — ликвидность и издержки)
- **Impact-cost gate**: оцениваем импакт по стакану `/v5/market/orderbook` и пропускаем сделку, если ожидаемая просадка > порога.
- **Dynamic fee-rate**: подтягиваем актуальные `maker/taker` по символу через `/v5/account/fee-rate` и учитываем в EV.
- **FIFO учёт PnL (спот)** — переключаемся на FIFO-отчёт наряду со средней стоимостью.
- **Мин. правила спота**: проверяем `tickSize/qtyStep/minNotional` из `/v5/market/instruments-info`.
- **Impact Analyzer** страница для ручной оценки VWAP/импакта.


### В v10 (SPOT — EV=EV(VWAP))
- **EV-фильтр теперь динамический**: используем оценку VWAP-impact из стакана как слиппедж на вход/выход → EV по реальным издержкам.
- **Impact-aware sizing**: автоматически уменьшаем размер входа, пока импакт не уложится в лимит.
- **Валидация правил спота**: квантование цены/объёма и проверка minNotional/minQty по `/v5/market/instruments-info`.
- **Динамический fee-rate** берём из `/v5/account/fee-rate` и кэшируем на 15 минут.
- **Лёгкий rate limiting** на шумные REST-эндпоинты (orderbook/kline/fee-rate/instruments).
- Новая страница **🧪 EV по VWAP/Impact** — подбор объёма под нужный EV.


### В v11 (SPOT — авто-тюнинг и лог качества исполнения)
- **Decision ledger**: перед размещением ордера записываем mid/ожидаемый импакт/EV → позже сравним с фактическими fill'ами.
- **Realized impact report**: сводка по последним сделкам, p75 импакта и предложенный порог.
- **EV Tuner (страница)**: одним кликом применяет новый лимит импакта в настройки.
- **Dynamic RR**: RR зависит от ADX (тренда), с границами в настройках.


### В v12 (SPOT — адаптация на собственных данных)
- Пер‑символьные overrides (лимит импакта, buy_threshold, RR).
- Автоподстройка buy_threshold по последним решениям.
- Отчёт по Implementation Shortfall (реальный импакт vs mid).
- Ручной рефреш правил спота (инструменты) и страница управления overrides.
- Заложен kill‑switch контур и лимит на число сделок в день.


### В v13 (SPOT — надёжность и дисциплина)
- **Kill‑switch**: пауза торговли при достижении дневного лимита сделок или при срабатывании ручного стопа; контур для оценки streak/реализованного PnL заложен.
- **Daily trade cap**: фактическое ограничение на число решений в сутки.
- **Rules refresher**: авто‑кэш спецификаций инструментов (6 часов) + ручная кнопка.
- **WS keepalive**: добавлен `max_active_time=10m` в URL WS (Bybit v5).
- **Ликвидность Sampler**: быстрая кривая impact(qty) для настройки порогов.
- (Примечание) В UTA Spot Bybit добавил TPSL‑поля к лимит‑ордерам; мы по‑прежнему ведём client‑OCO, чтобы явно контролировать ноги.


### В v14 (SPOT — серверный TP/SL и контроль)
- **Server-side TP/SL (Spot UTA)**: для лимитных спот-ордеров передаём `takeProfit/stopLoss` и `tpOrderType/slOrderType` (можно `Market` или `Limit`). Есть фоллбэк на клиентский OCO.
- **Market с ограничением слиппеджа**: helper для `slippageTolerance*` (TP/SL вместе с этим не поддерживаются — по доке).
- **UI Kill-Switch**: кнопки паузы/снятия паузы, индикатор состояния.
- **API‑Nanny**: счётчик вызовов REST в сессии процесса.
- Поля настроек: `spot_server_tpsl`, `spot_tpsl_tp_order_type`, `spot_tpsl_sl_order_type`.


### В v15 (SPOT — контроль результата и массовое исполнение)
- **Ex‑post EV kill‑switch**: считаем средний EV (bps) по реальным закрытиям (sell) против mid решения; при деградации ставим паузу на N минут.
- **Batch TWAP**: отправка до 10 IOC‑лимиток за один вызов `/v5/order/create-batch` для спота.
- **Idempotency**: все заявки получают уникальный `orderLinkId` (помогает отменять/диагностировать и избегать дублей).
- **EV‑Guard в UI**: кнопка проверки и авто‑паузы.
- **TWAP: batch‑режим из UI**.


### В v16 (SPOT — портфельная дисциплина)
- **Лимит общего капитала** в работе (portfolio_max_usdt_pct) + **лимит на группу скоррелированных символов** (corr>=threshold → не более group_max_cap_pct от Equity).
- **Vol‑target sizing**: аллокация ~ target_vol / symbol_vol (дневная вола из 1h‑рядов), с нормировкой под портфельный лимит.
- **Корреляционная матрица** (30 дней, 1h) и страница **🧮 Portfolio Risk**.
- **Ex‑ante проверки экспозиции**: перед размещением сделки уменьшаем объём, чтобы не нарушить символ/группу/портфель.
- Используем официальные эндпоинты: `/v5/market/kline`, `/v5/market/instruments-info`, `/v5/market/orderbook`, `/v5/account/fee-rate`.


### В v17 (SPOT — точный ex‑post и портфельная метрика ENB)
- **Pairing вход→выход** по споту: строим пары сделок и считаем `bps_realized`, `R‑multiple`, `hold_sec`.
- **Kill‑switch по R**: авто‑пауза, если средний R на последних трейдах ниже порога.
- **ENB (Effective Number of Bets)**: считаем по собственным значениям корр‑матрицы, показываем в Portfolio Risk.
- UI‑страница **🔗 Trade Pairs (Spot)** для прозрачноcти ex‑post EV.


### В v18 (SPOT — TIF, Iceberg, правки лимиток, быстрые отмены, IP‑лимиты)
- **TIF для лимит‑заявок**: PostOnly / GTC / IOC / FOK — выбирается в UI и передаётся в вызов `/v5/order/create` (PostOnly — строго мейкер). 
- **Client‑side Iceberg (Spot)**: стратегия разбивки большого ордера на под‑заявки с режимами `fast/better/fixed` и опциональным `price_limit`.
- **Amend Order**: поддержка `/v5/order/amend` для корректировок лимитки/TP/SL на UTA‑споте.
- **Cancel All / Batch Cancel**: быстрые кнопки для спота, в т.ч. по `orderLinkId`.
- **IP rate‑limiter**: встроенный токен‑бакет ~600 запросов за 5 сек на IP (легко меняется в коде).


### В v19 (SPOT — точность, устойчивость и гигиена)
- **Decimal‑квантование** цены/кол-ва + шаги инструмента → никаких плавающих хвостов.
- **Мин. нотионал:** соблюдаем `max(exchange_minNotional, 5 USDT)` (официальный порог для API), и `minQty` по сетке.
- **Временное окно:** страница Health показывает дрифт относительно `/v5/market/time`; в `envs` добавлен `recv_window_ms`.
- **API backoff:** `_safe_req` с экспоненциальным бэкоффом по `retCode` `10006/10016`.
- **SlippageTolerance для MARKET** (Spot) — управляемое через `ai_market_slippage_pct`.
- **Universe Builder:** фильтр по спрэду (bps) помимо оборота 24h — меньше проскальзывания, лучше исполнение.
- **orderLinkId ≤ 36** символов — соблюдение правил биржи.


### В v20 (SPOT — живой стакан, умный юниверс и защита от проскальзывания)
- **Live OB Impact**: VWAP‑оценка по живому стакану (готово к WS, фоллбэк — REST `/v5/market/orderbook`, до 1000 уровней).
- **Auto‑Rotate Universe**: раз в ~сутки подбираем ликвидные USDT‑пары по 24h‑обороту и спрэду, добавили **whitelist/blacklist** и **liquidity score**.
- **Pre‑trade Slippage Guard**: если ожидаемый слиппедж > `ai_max_slippage_bps`, уменьшаем объём до бюджета.
- **TTL‑кэш** для конфигов, чтобы не дёргать REST сверх меры.
- Всё совместимо с предохранителями: rate‑limit, idempotency (`orderLinkId`≤36), `recvWindow`, валидатор минималок ≥ **5 USDT**.


### В v21 (SPOT — реальный WS стакан, приватный WS-зёрна, лимиты и защита новых листингов)
- **Публичный WS стакан (v5)**: обрабатываем `snapshot`/`delta` по правилам Bybit; поддержка уровней 50/200/1000. REST остаётся фоллбэком.
- **Приватный WS (заготовка)**: аутентификация и подписка на `order`/`execution` — для будущих real‑time подтверждений вместо опросов.
- **Няня лимитов ордеров**: мониторинг спотовых капов (до 500 открытых, до 30 TP/SL, до 30 условных) + страница с показателями.
- **Guard «первые 5 минут листинга»**: по `/v5/market/instruments-info` определяем свежесть инструмента и гасим агрессивные типы (MARKET) в этот период.
- Всё совместимо с предыдущими предохранителями: minNotional ≥ 5 USDT, recvWindow/тайм‑синк, batch‑place/cancel (до 10), slippageTolerance и VWAP‑гарды.

# Copy this file to `.env` and replace the demo values with your real keys.
# The Streamlit UI stores runtime secrets in `.runtime/settings.json` (or
# profile-specific variants), so credentials saved via the UI are picked up
# automatically when `.env` is absent.

BYBIT_API_KEY=demo_key
BYBIT_API_SECRET=demo_secret
BYBIT_TESTNET=1
# Set BYBIT_ENV to "test" or "prod" to switch networks without touching code.
#BYBIT_ENV=test
BYBIT_DRY_RUN=0
# Optional overrides:
AI_ENABLED=1
AI_MAX_SPREAD_BPS=500
# TG_BOT_TOKEN=
# TG_CHAT_ID=
